/// <reference types="react-native" />
/// <reference types="node" />
declare class CountDownClass {
    timer: number;
    interval?: ReturnType<typeof setInterval>;
    onTimerEnd?: {
        (): void;
    }[];
    onTimerChange?: {
        (timer: number): void;
    }[];
    constructor();
    private floorDuration;
    private startTimer;
    private clearInterval;
    private handleOnTimerEnd;
    private handleOnTimerChange;
    private setDefaultTimerCallback;
    setTimer(duration: number): void;
    getTimer(onTimerChange: (timer: number) => void): void;
    getTimerOnEnd(onTimerEnd: () => void): void;
    resetTimer(): void;
}
export declare const CountDown: CountDownClass;
export {};
